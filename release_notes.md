# v1.6.5 更新日志

## ✨ 新功能

### 定时任务新增「执行目标」选项：可设置仅抓取数据

此前定时任务默认总是「抓取数据 + 分析黑词并提取」，无法只抓取数据而不做黑词分析。本次新增执行目标选择：

- **抓取数据 + 分析黑词并提取**（默认）— 抓取后自动进行黑词分析并写入词库（需配置模型 API），保持原有行为
- **仅抓取数据** — 只抓取并保存数据，跳过工作流分析与词库写入，无需模型 API

**交互细节：**

- 任务配置表单新增「执行目标」下拉框，带说明提示文字
- 选择「仅抓取数据」时，自动隐藏「分析工作流」勾选区与舆情配置字段（这些仅在分析模式下有意义）
- 定时任务列表在任务类型旁显示模式徽章（`含分析` / `仅抓取`），一目了然
- 编辑任务时正确回填执行目标；新建任务默认为「抓取+分析」

**技术实现：**

- `Pipeline.run()` 新增 `analyze: bool = True` 参数，为 `False` 时在采集完成后直接返回，跳过工作流分析（阶段二）与词库写入（阶段三）
- 定时任务执行逻辑根据 `params.analyze` 选择流水线：仅抓取模式使用默认流水线（不调用 LLM）
- 执行目标存储于任务的 `params.analyze` 字段，**无需数据库迁移**；历史任务缺省该字段时默认为 `True`，完全兼容旧数据
- 新增 2 个单元测试覆盖仅抓取模式与默认分析模式

---

## 🎨 界面深度美工改造

本版本同时包含一轮完整的界面视觉升级（分三阶段）：

**主色系**：以品牌色 `#A1CECD` 为核心的清亮青绿体系，左侧导航栏采用 `#A1CECD` 浅色背景 + 深青墨文字。

**阶段 A · 基础质感**
- 侧栏加分隔线 + 微投影，与内容区清晰脱开
- 页面标题排版层级强化（27px / 750 字重）
- 状态色与品牌色拉开（`已通过`改用正绿 `#16A34A`）
- 搜索表单配置行改为下沉式控制条，重心平衡

**阶段 B · 内容质感**
- 空状态插画：图标 + 引导文案 + 快捷按钮（仪表盘/词库/定时任务三处）
- Dashboard 统计卡新增彩色图标 + hover 上浮
- 微交互：统计数字滚动动画、表格行品牌色高亮

**阶段 C · 结构升级**
- 新增全局顶栏：面包屑导航 + 主题一键切换 + 设置快捷入口
- 词库管理筛选栏改为可折叠抽屉（带激活数量徽章），减少拥挤
- 响应式兜底：窄窗口自动收缩侧栏为图标栏，防止布局崩坏
- 深浅双主题完整适配，中英文 i18n 同步

---

## 📁 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/orchestrator/pipeline.py` | `run()` 新增 `analyze` 参数，`False` 时跳过分析与词库写入 |
| `src/api/main.py` | 定时任务执行读取 `params.analyze`，据此选择流水线并传参 |
| `static/index.html` | 定时任务表单新增「执行目标」选择与联动逻辑、列表模式徽章；界面深度美工改造（主色系、顶栏、空状态、筛选抽屉、响应式等） |
| `tests/test_pipeline.py` | 新增仅抓取模式 / 默认分析模式两个单元测试 |
| `VERSION` | `1.6.4` → `1.6.5` |

---

## 📌 前序版本回顾

- **v1.6.4** — 修复加速模式复选框重复；用户主页抓取新增排序与加速模式
- **v1.6.3** — macOS 安装包内置 Chromium Headless Shell，X/Twitter 开箱即用
