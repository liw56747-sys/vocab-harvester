# v1.7.2 更新日志

## ✨ 新增功能：定时任务完成弹窗提示

定时任务执行结束后，界面会**自动弹出提示框**告知结果，无需你盯着任务列表手动刷新。

### 效果
- **无论**是到点自动执行还是点「立即执行」，任务**跑完后都会弹窗**
- **无论**你当时停留在哪个页面，弹窗都会出现（全局提示）
- ✅ **成功**：显示「定时任务已完成」+ 任务名 + 「本次抓取 N 条数据」（0 条时提示未抓取到数据）
- ❌ **失败**：显示「定时任务执行失败」+ 任务名 + **具体失败原因**（如 Cookie 未配置/过期、网络异常等）

### 贴心细节
- **不会漏报**：任务的成功、失败、异常三种结束情况都会提示
- **不会重复弹**：每条通知只弹一次；应用启动时对齐到最新，不回放历史旧任务
- **不打扰**：多个任务同时完成时排队逐一弹出；后台轮询失败静默重试，不会打扰你

---

## 🔧 技术实现

- `src/api/main.py`：新增定时任务完成通知队列 `_push_scheduled_notification()`，在任务的三个结束点（缺 Cookie 提前返回、正常完成、异常）均记录通知；新增 `GET /api/scheduled-notifications?after=<id>` 接口
- `static/index.html`：新增全局轮询（每 10 秒）拉取新完成通知并弹窗，含成功/失败两种样式，中英双语
- 端到端验证：任务完成后接口正确返回通知（含任务名/状态/失败原因），且已弹过的不再重复

---

## 📁 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/api/main.py` | 定时任务完成通知队列 + `/api/scheduled-notifications` 接口 + 三处结束点埋点 |
| `static/index.html` | 全局轮询弹窗、弹窗样式、中英文案 |
| `VERSION` | `1.7.1` → `1.7.2` |

---

## 📌 前序版本回顾

- **v1.7.1** — 手动搜索自动同步 Cookie 到服务端，与定时任务共用同一份
- **v1.7.0** — 抓取失败透传真实原因 + 定时任务状态实时展示与失败原因
