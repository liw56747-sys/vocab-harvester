# v1.7.1 更新日志

## 🐛 问题修复：手动与定时任务现在共用同一份 Cookie

### 问题现象
手动抓取能拿到真实数据，但定时任务却报「所选平台均未配置 Cookie」而失败。

### 根本原因
Cookie 存在**两个位置**且未真正打通：
- **手动抓取**：用前端 localStorage 里的 Cookie（随请求发给后端）
- **定时任务**：后台运行、无前端，只能读服务端数据库 `platform_cookies`

v1.6.9 虽然在"点击保存 Cookie"时会同步到服务端，但如果用户的 Cookie 是更早版本存的、之后没再点过「保存」，服务端数据库就是**空的** → 定时任务拿不到 Cookie → 失败。经核查，用户的服务端 Cookie 表确实为空，而手动搜索一直用的是 localStorage 的 Cookie，二者未同步。

### 修复
**手动搜索/抓取时，自动把正在使用的 Cookie 同步到服务端**，确保手动与定时任务始终共用同一份最新 Cookie：
- `/api/search`（关键词搜索）、`/api/batch-search`（批量搜索）、`/api/twitter-fetch`（用户主页抓取）在执行前，都会自动把请求里的 Cookie 写入服务端 `platform_cookies` 表
- 只在字段有效时写入（避免空值覆盖），异常静默忽略（不影响本次抓取）
- 从此你只要用过一次手动搜索，定时任务就能自动用上同一份 Cookie，无需手动"保存同步"

### 立即生效方式（针对老用户）
升级到 v1.7.1 后，**随便做一次手动搜索**（或在 Cookie 认证页重新点一次「保存」），服务端就会有 Cookie，定时任务即可正常真实抓取。

---

## 🔧 技术实现

- `src/api/main.py`：新增 `_auto_persist_cookies()`，在关键词搜索、批量搜索、用户主页抓取三个入口自动持久化平台 Cookie 到 `platform_cookies` 表
- 全套测试 **70 passed**；实测：手动搜索后服务端 Cookie 由空变为已配置

---

## 📁 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/api/main.py` | 新增 `_auto_persist_cookies()`；`/api/search`、`/api/batch-search`、`/api/twitter-fetch` 执行前自动同步 Cookie 到服务端 |
| `VERSION` | `1.7.0` → `1.7.1` |

---

## 📌 前序版本回顾

- **v1.7.0** — 抓取失败透传真实原因 + 定时任务状态实时展示与失败原因
- **v1.6.9** — 定时任务接入真实抓取（Cookie 服务端持久化 + 真实爬虫）
